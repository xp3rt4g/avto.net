<%= form_with(model: fuel_type, local: true) do |form| %>
  <% if fuel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fuel_type.errors.count, "error") %> prohibited this fuel_type from being saved:</h2>

      <ul>
        <% fuel_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
