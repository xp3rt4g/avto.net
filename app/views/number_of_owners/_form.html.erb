<%= form_with(model: number_of_owner, local: true) do |form| %>
  <% if number_of_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(number_of_owner.errors.count, "error") %> prohibited this number_of_owner from being saved:</h2>

      <ul>
        <% number_of_owner.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
