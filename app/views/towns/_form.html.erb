<%= form_with(model: town, local: true) do |form| %>
  <% if town.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(town.errors.count, "error") %> prohibited this town from being saved:</h2>

      <ul>
        <% town.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :post_number %>
    <%= form.number_field :post_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
