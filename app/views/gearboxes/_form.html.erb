<%= form_with(model: gearbox, local: true) do |form| %>
  <% if gearbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gearbox.errors.count, "error") %> prohibited this gearbox from being saved:</h2>

      <ul>
        <% gearbox.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
